const JSX = (
  <div>
    <h1>Hello JSX!</h1>
    <p>JSX is a syntax extension to JavaScript.</p>
    <ul>
      <li>JSX is a syntax extension to JavaScript.</li>
      <li>JSX is a syntax extension to JavaScript.</li>
      <li>JSX is a syntax extension to JavaScript.</li>
    </ul>
  </div>
);
